{
  "id": "evopaimo",
  "kind": "channel",
  "channels": ["evopaimo"],
  "name": "EvoPaimo",
  "description": "EvoPaimo desktop pet relay channel plugin. Connects to Cloudflare Workers via WebSocket and dispatches client messages into the local OpenClaw agent runtime.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "evopaimo": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "relayUrl": {
            "type": "string",
            "description": "HTTPS base URL of the EvoPaimo relay (e.g. https://primo.evomap.ai). The plugin will derive wss://.../ws/openclaw from this."
          },
          "linkCode": {
            "type": "string",
            "description": "6-character link code provided by the EvoPaimo desktop client."
          },
          "secret": {
            "type": "string",
            "description": "Paired secret provided alongside the link code."
          },
          "sessionLabel": {
            "type": "string",
            "description": "OpenClaw session label used when dispatching messages to the agent. Defaults to 'mobile-app'.",
            "default": "mobile-app"
          },
          "emotionWrapperEnabled": {
            "type": "boolean",
            "description": "Whether to wrap inbound user messages with the EvoPaimo emotion-prompt template before agent dispatch. Defaults to true.",
            "default": true
          },
          "allowFrom": {
            "type": "array",
            "description": "Reserved; EvoPaimo defers authentication to the Cloudflare Workers relay (via linkCode+secret), so this is unused in the default configuration.",
            "items": { "type": "string" },
            "default": []
          }
        },
        "required": ["relayUrl", "linkCode", "secret"]
      }
    }
  }
}
